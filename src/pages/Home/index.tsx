import { Header } from '../../components/Header';
import * as Styled from './styled';

export const Home = () => {
  return (
    <Styled.Container>
      <Header />
      <Styled.Section></Styled.Section>
    </Styled.Container>
  );
};
